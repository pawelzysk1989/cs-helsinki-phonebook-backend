{
  "name": "cs-helsinki-phonebook-backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "ts-node index.ts",
    "dev": "nodemon index.ts",
    "prod": "node index.js",
    "build:server": "tsc",
    "build:ui": "cd ../fullstack-cs-helsinki/part2/phonebook && npm run build --prod && cp -r dist ../../../cs-helsinki-phonebook-backend",
    "build": "npm run clean && npm run build:server && npm run build:ui",
    "clean": "rm -rf dist index.js",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build && git add . && git commit -m uibuild && git push && npm run deploy && npm run clean",
    "logs:prod": "heroku logs --tail"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pawelzysk1989/cs-helsinki-phonebook-backend.git"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/morgan": "^1.9.3",
    "@types/node": "^16.11.7",
    "nodemon": "^2.0.14",
    "ts-node": "^10.4.0",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  }
}
